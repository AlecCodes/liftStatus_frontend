//this function should recieve an object(month data), a date and return the 'report card' of given date.

export async function dayReportCard(monthData){
    const response = monthData
    console.log(response)
}